# 用 ArkUI 写微信小程序：ArkMP 编译器入门指南

> 如果你喜欢 ArkUI 的声明式语法，又需要交付微信小程序——ArkMP 让你鱼与熊掌兼得。

## 一个尴尬的现实

鸿蒙生态里，ArkUI 是目前最顺手的声明式 UI 框架。`@State` 驱动响应式更新，`build()` 里链式调用搭积木一样拼界面，写起来行云流水。

但现实是：**国内绝大多数前端项目仍然要交付微信小程序。**

于是你面临一个选择——

- 用原生小程序开发？WXML + WXSS + JS 三件套，手写 `setData`，状态管理全靠人肉。
- 用 Taro 或 uni-app？它们很好，但语法来源是 React/Vue，和 ArkUI 的开发体验完全是两回事。

**如果你已经熟悉了 ArkUI，有没有办法直接用它来写小程序？**

有。这就是 **ArkMP** 做的事。

## ArkMP 是什么

一句话定义：

> **ArkMP 是一个编译工具：输入 ArkUI / ArkTS 声明式语法，输出标准微信小程序工程。**

类比来说，它和 Taro 的定位类似——都是「用一种语法编译到小程序」。区别在于：Taro 的语法来源是 React，而 ArkMP 的语法来源是 ArkUI。

整个流程长这样：

```text
你的 .ets 源码（ArkUI 语法）
        │
   ArkMP 编译器
        │
   parse → analyze → IR → transform → emit
        │
  标准微信小程序工程（WXML / WXSS / JS / JSON）
```

你写的 `.ets` 文件经过编译，直接变成可以用微信开发者工具打开的标准小程序工程。不需要 React，不需要 Vue，就是 ArkUI 原生语法。

## 五分钟看懂：一段 ArkUI 代码变成什么

光说不练假把式。我们来看一个真实的例子——一个带状态管理和子组件的计数器页面。

### 输入：ArkUI 源码

页面组件 `Index.ets`：

```ts
@Entry({ title: '首页' })
@Component
struct Index {
  @State count: number = 0;

  build() {
    Column({ space: 12 }) {
      UserCard({ name: 'ArkMP' })
      Text(`count=${this.count}`).fontSize(20)
      Button('点我 +1').onClick(() => {
        this.count++;
      })
    }
  }
}
```

子组件 `UserCard.ets`：

```ts
@Component
struct UserCard {
  name: string = '';

  build() {
    Row() {
      Text(this.name)
    }
  }
}
```

如果你写过 ArkUI，这段代码不需要任何解释——声明状态、搭建 UI、绑定事件，一气呵成。

### 输出：标准小程序工程

ArkMP 编译后，你会得到一个完全标准的小程序目录：

```text
dist/
├── app.json                  # 小程序全局配置
├── app.wxss                  # 全局样式
├── arkmp/
│   └── runtime.js            # 运行时（< 10KB，负责状态桥接）
├── pages/
│   └── index/
│       ├── index.wxml        # 页面模板
│       ├── index.wxss        # 页面样式
│       ├── index.js          # 页面逻辑
│       └── index.json        # 页面配置
└── components/
    └── user-card/
        ├── user-card.wxml
        ├── user-card.js
        └── user-card.json
```

逐个打开看看：

**`index.wxml`** —— `build()` 里的组件变成了 WXML 模板：

```html
<!-- Generated by ArkMP, do not edit -->
<view class="arkmp-col" style="gap: 24rpx">
  <user-card name="ArkMP" />
  <text class="arkmp-n0">count={{count}}</text>
  <view class="arkmp-btn" bindtap="__n0_click">点我 +1</view>
</view>
```

**`index.js`** —— `@State count` 变成了 `data`，事件回调由运行时桥接：

```js
// Generated by ArkMP, do not edit
const { createPage } = require('../../arkmp/runtime.js');

createPage({
  state: {
    count: 0,
  },
  __n0_click() {
    this.count++;
  },
});
```

**`index.wxss`** —— 链式样式变成了 CSS 类，`fontSize(20)` 自动换算为 `40rpx`：

```css
.arkmp-col {
  display: flex;
  flex-direction: column;
}

.arkmp-n0 {
  font-size: 40rpx;
}
```

**`index.json`** —— `@Entry` 的参数变成了页面配置，`usingComponents` 自动生成：

```json
{
  "navigationBarTitleText": "首页",
  "usingComponents": {
    "user-card": "/components/user-card"
  }
}
```

注意到了吗？**整个编译过程是「深转换」**——`build()` 直出 WXML，组件映射、事件绑定、样式换算全部在编译期完成。运行时只负责一件事：状态桥接（把 `this.count++` 映射到小程序的 `setData`）。

这意味着产出的代码干净、可读，不依赖庞大的框架运行时。

## 它是怎么做到的：编译管线揭秘

ArkMP 的编译过程分五步，每一步都是纯函数、无副作用：

```text
.ets 源码
   │
   ① 解析（parse）
   │   ArkUI 的 struct 语法不是标准 TypeScript，ArkMP 会先把它预处理成
   │   标准 class，再用 TypeScript Compiler API 解析成 AST。
   ▼
   ② 分析（analyze）
   │   从 AST 中提取组件模型：哪些字段是 @State、哪些是 props、
   │   build() 的结构长什么样、绑定了哪些事件。
   ▼
   ③ 中间表示（IR）
   │   生成与具体平台无关的 ComponentModel——states、props、
   │   lifecycle、buildTree、styles、events。这一步把所有
   │   ts.Node 序列化掉，后续阶段不再碰 TypeScript。
   ▼
   ④ 转换（transform）
   │   四条并行转换链同时工作：
   │   ├─ buildTree  → WXML（组件映射 + wx:if/wx:for + {{}} 绑定）
   │   ├─ styles     → WXSS（样式类 + vp→rpx 单位换算）
   │   ├─ state      → data + __set 桥接 + createPage/createComponent
   │   └─ events     → 事件表 + 回调方法抽取
   ▼
   ⑤ 输出（emit）
   │   生成 wxml/wxss/js/json 四件套，注入 runtime，写入 dist/
```

设计上有两个关键决策值得拿出来说：

**第一，IR 与平台解耦。** 所有 ArkUI 特有的语法（装饰器、链式调用、struct）在分析阶段就被序列化成通用的中间表示。这意味着如果将来要支持支付宝小程序或百度小程序，只需要新增一条 transform 链，前面的解析和分析完全复用。

**第二，编译期做重，运行时做轻。** 与 React 那种「运行时虚拟 DOM diff」不同，ArkMP 在编译期就把组件树拍平成静态 WXML 模板。运行时只做状态→数据的映射，所以运行时文件不到 10KB，首屏加载没有框架开销。

## 运行时：不到 10KB 的状态桥接

上面反复提到「运行时」，它到底做了什么？三件事：

### 1. 状态桥接

ArkUI 里你可以直接写 `this.count = 1`，框架自动刷新界面。小程序里你得手动调 `this.setData({ count: 1 })`。ArkMP 的运行时帮你抹平了这个差异：

```text
ArkUI 写法          小程序实际执行
─────────────       ──────────────────
this.count++   →    this.setData({ count: newValue })
```

更妙的是，同一时刻多次修改状态会被自动合并为一次 `setData` 调用（microtask 批量调度），避免频繁渲染导致的性能问题。

### 2. 生命周期映射

ArkUI 的生命周期和小程序的钩子函数名称不同，运行时自动做了映射：

| ArkUI | 微信小程序 |
| --- | --- |
| `aboutToAppear` | `onLoad` |
| `onPageShow` | `onShow` |
| `onDidBuild` | `onReady` |
| `onPageHide` | `onHide` |
| `aboutToDisappear` | `onUnload` |

### 3. 派生字段与 @Watch

`@State` 的派生计算和 `@Watch` 监听也在运行时处理。当 `count` 变化时，依赖它的派生字段会自动重算，注册了 `@Watch` 的回调会自动触发——这一切都不需要你手写。

## 快速上手：三步跑起来

### 第一步：安装

```bash
npm install -g @arkmp/cli
```

要求 Node.js ≥ 18。

### 第二步：创建工程

```bash
ark-mp init my-app
cd my-app
```

ArkMP 提供两个模板：`default`（极简起步）和 `demo`（含多页面 + 自定义组件 + tabBar 的完整示例）。

```bash
# 用 demo 模板创建
ark-mp init my-app --template demo
```

### 第三步：开发与构建

```bash
# 开发模式：watch 监听文件变化，增量编译，自动唤起微信开发者工具
ark-mp dev

# 全量构建：编译输出标准小程序到 dist/
ark-mp build
```

`dist/` 目录可以直接用微信开发者工具打开预览。

此外还有几个实用命令：

```bash
ark-mp compile src/components/UserCard.ets --out ../existing-mp/components/
# 单文件编译，方便渐进式接入已有的小程序项目

ark-mp check
# 只跑诊断检查，不生成产物——适合集成到 CI

ark-mp doctor
# 环境检查：Node 版本、微信开发者工具 CLI、appId 配置
```

## 为什么选择 ArkMP

市面上已经有 Taro、uni-app、mpvue 等小程序框架，ArkMP 的差异化价值在于：

- **语法原生**：不是「在 ArkUI 上套了一层壳」，而是直接支持 ArkUI 声明式语法。装饰器、链式 UI、`@State` 响应式——原汁原味。
- **编译期深转换**：`build()` 在编译期直出 WXML，不是运行时动态渲染。产出的是干净、静态的模板文件，对小程序的性能优化机制（如初始渲染缓存）友好。
- **运行时极轻**：整个运行时不到 10KB，只做状态桥接。不打包组件框架，不注入虚拟 DOM。
- **产物标准、无锁定**：生成的就是标准 WXML/WXSS/JS/JSON，没有任何私有格式。即使将来不想用 ArkMP 了，产物本身可以脱离工具独立维护。
- **渐进式接入**：不需要一次性迁移整个项目。`ark-mp compile` 支持单文件编译，可以先在一个组件上试水。

## 谁适合用

- **鸿蒙开发者转小程序**：你已经熟悉 ArkUI，不想再学一套 WXML 语法。ArkMP 让你的 ArkUI 技能直接产出小程序。
- **追求开发体验的小程序团队**：ArkUI 的声明式 + 响应式状态管理比手写 `setData` 高效得多，而且类型安全。
- **多端技术探索者**：如果你在评估「统一跨端方案」，ArkMP 提供了一条不同于 React/Vue 技术栈的路径。

## 写在最后

ArkMP 的核心理念很简单：**你用最顺手的语法写，编译器负责翻译成最优的产物。**

它不造新框架，不发明新语法，只是把 ArkUI 这套已经在鸿蒙生态验证过的开发体验，带到了微信小程序的世界。

项目完全开源，欢迎 Star、Issue 和 PR：

**GitHub: [https://github.com/BoltDoggy/arkmp](https://github.com/BoltDoggy/arkmp)**

```bash
# 克隆并本地体验
git clone https://github.com/BoltDoggy/arkmp.git
cd arkmp
pnpm install
pnpm build
pnpm test   # 295+ 测试用例，涵盖编译管线全链路
```

如果你觉得这个项目有意思，给个 Star 是对开发者最好的鼓励。有任何问题或建议，欢迎在 GitHub Issues 讨论。

---

*ArkMP —— 让 ArkUI 的优雅，流淌进微信小程序。*
